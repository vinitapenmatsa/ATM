1. find a better way to insert data ( insert only if data does not exist)
3. use config file for dev , prod configurations
4. Check all response codes and messages // 400 might not be right for all validations


//Minimum coin change algorithm
-> The naive way to think about this is to just get the maximum denomination and find how many times it is divisible by the amount and then go on. But this will not work because the reminder might not be possible. so we need to kind of back track and see what values are possible.

-> Explain canonical and non - canonical and try to implement an algorithm to determine this.

therefore - > greedy

-> remove all hardcoding
